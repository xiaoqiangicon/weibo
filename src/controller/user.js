/**
 * @description user controller
 */

/**
 * 
 * @param {string} userName 用户名 
 */
async function isExist (userName) {
  // 业务逻辑处理
  // 调用service层获取数据
  // 统一返回格式
  
}

module.exports = {
  isExist,
}